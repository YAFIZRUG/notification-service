# Notification Service (`notification-service`)

Микросервис для отправки email-уведомлений. Получает события о пользователях из Kafka и отправляет приветственные письма или письма об удалении аккаунта.

## 📦 Технологии
*   **Java 17**, **Spring Boot 3.3.2**
*   **Spring Kafka** (Consumer)
*   **Spring Mail** (интеграция с Gmail SMTP)
*   **Spring Web** (REST API)

## 🚀 Запуск
1.  Убедитесь, что **Kafka** запущена.
2.  Настройте почтовый сервер (Gmail) в `src/main/resources/application.properties`:
    ```properties
    spring.mail.username=ваш_email@gmail.com
    spring.mail.password=ваш_пароль_приложения
    ```
3.  Соберите и запустите приложение:
    ```bash
    mvn clean spring-boot:run
    ```
4.  Сервис будет доступен по адресу: `http://localhost:8081`

## 🔗 Интеграция с Kafka
Сервис подписан на топик **`user.events`** (группа `notification-group`) и автоматически отправляет email при получении события.

## 📡 REST API (дополнительный)
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST   | `/api/notifications/send` | Отправить email уведомление напрямую. |
| GET    | `/api/notifications/health` | Проверка здоровья сервиса. |

**Пример запроса на прямую отправку:**
```bash
curl -X POST http://localhost:8081/api/notifications/send \
  -H "Content-Type: application/json" \
  -d '{"email":"user@example.com","operation":"CREATED"}'
